node_modules/
dist/
coverage/
*.vsix
.DS_Store
*.log

# Single package manager: npm (package-lock.json). Keep other lockfiles out.
pnpm-lock.yaml
yarn.lock

# Secret-scanner test fixtures — real-looking secrets kept locally, never commit
test/.env
test/.npmrc
test/credentials.json
test/id_rsa
test/server.pem
test/terraform.tfstate
test/docker-compose.yml
